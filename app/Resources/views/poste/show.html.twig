{% extends 'base.html.twig' %}

{% block body %}
    <h1>Poste</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ poste.id }}</td>
            </tr>
            <tr>
                <th>Libelleposte</th>
                <td>{{ poste.libellePoste }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('poste_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('poste_edit', { 'id': poste.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
