

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><i class="fa fa-times" aria-hidden="true"></i><span class="close"></span></button>
    <h2 class="modal-title" id="modal-fa5-label">Modifier evaluation </h2>
</div>
<div class="modal-body">
    {{ form_start(edit_form,{'attr':{'class':'smart-form','id':'smart-form-register','action':path('formateur_edit',{'id':formateur.id}),'method':'POST'|trans}})}}
<div class="row">
    <section class="col col-6">
        <label class="label">nom</label>
        <label class="input">
            <i class="icon-append fa fa-tag"></i>
            {{ form_widget(edit_form.nom,{'attr': {'type':'text','name':'site','placeholder':'prenom'|trans}}) }}
            {{ form_errors(edit_form.nom)}}
        </label>
    </section>
    <section class="col col-6">
        <label class="label">prenom</label>
        <label class="input">
            <i class="icon-append fa fa-tag"></i>
            {{ form_widget(edit_form.prenom,{'attr': {'type':'text','name':'site','placeholder':'prenom'|trans}}) }}
            {{ form_errors(edit_form.prenom)}}
        </label>
</section>

    <section class="col col-6">
        <label class="label">cv</label>

            {{ form_widget(edit_form.file)}}
            {{ form_errors(edit_form.file)}}
      </section>
</div>
</div>

<div class="modal-footer">
    {{ form_widget(edit_form._token) }}

    <footer>
        <button type="button" class="btn btn-default" data-dismiss="modal">
            Annuler
        </button>
        <button type="submit" class="btn btn-primary">
         Modifier
        </button>


    </footer>
    {{ form_end(edit_form,{'render_rest':false}) }}
</div>



