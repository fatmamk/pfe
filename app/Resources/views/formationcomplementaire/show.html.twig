{% extends 'base.html.twig' %}

{% block body %}
    <h1>Formationcomplementaire</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formationComplementaire.id }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ formationComplementaire.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('formationcomplementaire_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('formationcomplementaire_edit', { 'id': formationComplementaire.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
